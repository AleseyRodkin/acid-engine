[package]
name = "acid-judge"
version = "0.1.0"
edition = "2021"
rust-version = "1.78"
description = "AcidEngine verdict mirror: bind plan.lock then judge observation. Not a runtime, not phase 7 done."
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
