[package]
name = "acid-judge"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "AcidEngine external judge: bind plan.lock, run Python worker, verdict."
authors = ["Rodkin Aleksey"]
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
