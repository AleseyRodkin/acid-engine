[package]
name = "acid-judge"
version = "0.2.29"
edition = "2021"
rust-version = "1.75"
description = "Supervisor: bind plan.lock, run Python worker, verdict. Not a second body canon."
authors = ["Rodkin Aleksey"]
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
