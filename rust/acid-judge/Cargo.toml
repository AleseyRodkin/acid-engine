[package]
name = "acid-judge"
version = "0.1.0"
edition = "2021"
description = "AcidEngine judge: bind plan.lock and verdict. Not a runtime."
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
